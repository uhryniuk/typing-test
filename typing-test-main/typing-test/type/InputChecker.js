// Event Listeners and word testing.
class InputChecker{
    constructor(){
        console.log("Input Checker Connected ");
    }
    // Call this on interval to test letters and the space;
    testInputs(currentInput, currentWordID){
        let wordInput = document.getElementById("word-input").value;
        
    }
    // Need to make method for traversing to the next word 
    // As well as testing spaces too.
        // NOT FINISHED AT ALL
}